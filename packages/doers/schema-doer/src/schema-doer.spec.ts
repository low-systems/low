import { SchemaDoer } from './schema-doer';
import { ConnectorContext, Environment, TaskConfig } from 'low';

test('should be able to perform a Http request', async () => {
  jest.setTimeout(30000);

  expect(true).toBeTruthy();
});