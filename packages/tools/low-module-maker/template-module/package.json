{
  "version": "0.0.1",
  "main": "lib/connector-http.js",
  "types": "lib/connector-http.d.ts",
  "scripts": {
    "clean": "rm -rf docs/; rm -rf lib/; rm -rf coverage/",
    "test": "jest --coverage",
    "build": "npm run clean && tsc && npm test && typedoc"
  },
  "license": "GPL-3.0",
  "jest": {
    "preset": "ts-jest",
    "ts-jest": {
      "tsConfig": "tsconfig.json"
    }
  },
  "dependencies": {
    "low": "low"
  }
}
