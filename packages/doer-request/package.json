{
  "version": "0.0.1",
  "main": "lib/doer-request.js",
  "types": "lib/doer-request.d.ts",
  "scripts": {
    "clean": "rm -rf docs/; rm -rf lib/; rm -rf coverage/",
    "test": "jest --coverage",
    "build": "npm run clean && tsc && npm test && typedoc"
  },
  "license": "GPL-3.0",
  "jest": {
    "preset": "ts-jest",
    "ts-jest": {
      "tsConfig": "tsconfig.json"
    }
  },
  "dependencies": {
    "low": "../low",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.8"
  },
  "name": "@low-systems/doer-request",
  "description": "A Http Request doer for low",
  "homepage": "https://github.com/low-systems/low#readme",
  "author": {
    "name": "Matthew Wilkes (@SCVO)",
    "url": "https://scvo.org"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/low-systems/low"
  },
  "devDependencies": {
    "@types/request": "^2.48.3",
    "@types/request-promise-native": "^1.0.17"
  }
}
